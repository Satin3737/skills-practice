{
    "extends": ["../../tsconfig.json"],
    "compilerOptions": {
        "paths": {
            "@/*": ["./src/*"]
        }
    },
    "include": ["src", "tests", "vitest.config.ts", "scripts"],
    "exclude": ["src/database/prisma", "src/database/prismabox"]
}
